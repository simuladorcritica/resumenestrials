# CHANGELOG de diseño — Resúmenes Trials

Registro de cambios del rediseño estético (prompt v3), fase por fase. Comentarios en español. Ningún cambio se publica a `main` por esta vía; cada fase queda en una rama para revisión humana.

## Fase 0 — Mapa y decisión de arquitectura (sin cambios de código)

Ver `INFORME_FASE0_MAPEO.md` (entregado aparte). Resumen: se verificó el prompt v3 contra el código real; se corrigieron varios supuestos (39 ensayos, no 115; no existe `site-runtime.css`; 78 páginas `resumen/*.html` huérfanas; 19 estilos `rt-*` inyectados, no ~18) y se confirmaron los hallazgos críticos (failsafe de opacidad, bug de ancho, ~130 literales de color, 3 paletas superpuestas).

## Fase 1 — Fiabilidad primero (rama `fase-1-fiabilidad`)

**Qué cambió:** `future-experience-patch.css`, líneas 2-5.

**Por qué:** el body quedaba en `opacity:0` con una animación que solo lo revelaba a los 2.5s (`rt-legacy-failsafe`), y solo si `future-experience.js` llegaba a añadir la clase `.rt-future`. Si el script no cargaba o no ejecutaba (bloqueador de anuncios, red lenta, JS deshabilitado o roto), la página podía quedar en blanco. Esto va en contra del requisito de fiabilidad: "el HTML/CSS base debe verse bien sin JS".

**Qué se hizo:** se eliminó la regla `body:not(.rt-future){opacity:0;...}` y el `@keyframes rt-legacy-failsafe`. Ahora `body{opacity:1}` de forma incondicional. El HTML servido sin `.rt-future` ya tiene su propio diseño editorial completo (paleta clara, tipografía Fraunces/Newsreader/IBM Plex Mono ya presentes en el marcado base) — no dependía de JS para verse bien, solo para revelarse. El JS (`future-experience.js` y la cadena que añade `.rt-future*`) sigue funcionando exactamente igual como mejora progresiva hacia el tema oscuro "Future"; no se tocó ningún selector `.rt-future*` ni la lógica de detección de ruta.

**Qué se dejó intacto:** toda la lógica JS (`inferSubspecialty`, filtros, búsqueda, `jsPDF`/`sanPDF`, guardas de re-ejecución), `resumenes.json`, los 19 enganches `rt-*` inyectados, y el resto de `future-experience-patch.css` (glow del puntero, reglas de `.rt-future-*`, etc.).

**Verificación realizada:**
- `node --check` sobre los 14 scripts de la cadena "future" + pdf: sin errores (no se modificó JS).
- Revisión estática: ningún otro archivo CSS/JS depende de `rt-legacy-failsafe`, del `opacity:0` inicial, ni de un `body` invisible antes de `.rt-future` (se comprobó `future-experience.js` y toda la cadena `fix-v4*`/`reader-*`).
- No se corrió el smoke test con navegador real (`scripts/future-experience-smoke.mjs`, que usa Playwright) porque Playwright no está instalado en este entorno de trabajo; el cambio es una regla CSS de una línea sin animaciones ni casos borde adicionales. Recomiendo correr `.github/workflows/future-experience.yml` (o el smoke test local) sobre esta rama antes de fusionar, como hace el propio CI en cada PR.

**Pendiente / fuera de alcance de esta fase:** el resto de la deuda (paletas superpuestas, bug de ancho, tema claro/oscuro real, modo lectura, impresión) se aborda en las fases 2 a 5, cada una con su propio gate de aprobación.

## Fase 2 — Fuente única de tokens (rama `fase-2-tokens`, sobre `fase-1-fiabilidad`)

**Corrección a Fase 0:** el informe de Fase 0 decía que `trial.css` "no usa variables". Eso fue un falso negativo (el `:root{...}` de `trial.css` está en una sola línea larga y el patrón de búsqueda usado no lo detectó). En realidad `trial.css` ya tenía 11 tokens propios (`--tinta`, `--tinta-2`, `--teal`, `--teal-hondo`, `--papel`, `--papel-2`, `--linea`, `--linea-fuerte`, `--ambar`, `--interna`, `--blanco`) y los usa consistentemente. Además, sus valores de color **no son una variante de la paleta A con los mismos nombres** (como decía el prompt v3): son una familia de color propia y distinta ("tinta" más oscura y saturada, `#10253d` en vez de `#12233b`), que **coincide exactamente** con la paleta usada en `privacidad.html` (`--ink:#10253d`, `--paper:#f5f2e9`, etc.). Es decir, hay dos familias de color en uso, no una paleta "B" corrupta de la "A".

**Qué se hizo (cambios de bajo riesgo, verificados sin diferencia visual):**

1. `trial.css`: se tokenizaron los 9 colores sueltos que quedaban sin variable (`#fff`, y 8 colores de un solo uso en la barra superior, migas de pan y estados de hover) con nombres nuevos (`--blanco-puro`, `--topbar-ink`, `--topbar-ink-hover`, `--migas-ink`, `--migas-ink-2`, `--tinta-suave`, `--tinta-fuerte`, `--ambar-texto`, `--hover-ink-claro`). `trial.css` queda con **cero literales de color sueltos en el cuerpo de las reglas**; todo vive en su `:root`.
2. `future-experience.css`: se añadió el bloque de 8 tokens de la paleta clara (`--tinta`, `--tinta-2`, `--teal`, `--teal-hondo`, `--papel`, `--papel-2`, `--linea`, `--ambar`) a su `:root` ya existente, con los mismos valores que ya usaban `_includes/index-source.html`, `resumen.html` y `agregar.html`. Se reemplazaron los 4 literales que coincidían exactamente con tokens `--rt-*` ya declarados (`#06131f`→`--rt-bg` ×2, `#0a2133`→`--rt-panel`, `#f4f0e6`→`--rt-ivory`).
3. Se eliminó la declaración duplicada de esos 8 tokens en `_includes/index-source.html`, `resumen.html` y `agregar.html` (los tres tenían los mismos 8 valores copiados a mano). Ahora se sirven desde la única declaración en `future-experience.css`, que las tres páginas ya cargan. Se conservaron intactos los tokens propios de cada página que no formaban parte del duplicado (`--critica`/`--interna` en portada y resumen; `--err`/`--ok` en agregar).

**Verificación realizada (sin navegador, por resolución programática de variables):** se escribió un resolutor de `var(--x)` en Python y se comprobó, comparando contra el commit `b027453` (antes de cualquier cambio), que el CSS resuelto —es decir, los valores de color que efectivamente llegan a cada regla— es **byte a byte idéntico** al original en `future-experience.css`, `trial.css`, y en los tres `<style>` inline de portada/resumen/agregar (tomando los 8 tokens ahora externos de `future-experience.css`). Cero diferencias. También se validó la sintaxis de los 5 archivos con un parser CSS (`tinycss2`): 0 errores.

**Riesgo nuevo a tener en cuenta:** antes, si `future-experience.css` no llegaba a cargar, portada/resumen/agregar igual se veían bien porque cada una declaraba su propia copia de la paleta clara. Ahora dependen de que ese archivo cargue. Es la misma dependencia que ya tenían para el resto del sistema "Future" (layout, tipografía, etc.), así que no es un riesgo nuevo de categoría, pero si se quiere blindar más, se podría inlinear solo esos 8 tokens como fallback — no se hizo aquí para no reintroducir la duplicación que se acaba de quitar.

**Qué NO se tocó y por qué (pendiente de decisión, no ejecutado):**

- **~115 colores restantes en `future-experience.css`** no tienen equivalente exacto entre los tokens ya declarados: son variaciones muy cercanas entre sí (decenas de tonos casi-idénticos de blanco/gris-azulado/teal, con solo 1-2 dígitos hex de diferencia — p. ej. `#eef4f3`, `#eef4f2`, `#eef3ef`, `#eef2ef`...). Esto sugiere que no son 115 colores deliberadamente distintos, sino un número mucho menor de tonos reales que se fueron desviando con ediciones manuales repetidas. Consolidarlos de verdad implica agrupar esas variaciones en un conjunto menor de tokens, lo que cambiaría el valor exacto de algunos píxeles (de forma imperceptible, pero no es "cero diferencia" estricta como todo lo anterior). Antes de aplicar ese agrupamiento a ciegas en un sitio médico en producción, prefiero proponerlo con capturas de antes/después en vez de adivinar. Puedo preparar esa propuesta como siguiente paso si se aprueba.
- **Fragmentación adicional descubierta en `login.html`, `recuperar.html`, `registro.html`, `cuenta.html`, `biblioteca.html`, `privacidad.html`:** cada una declara su propio `:root` inline con la misma paleta de marca pero con **nombres de variable distintos** en cada archivo (`--tinta`/`--tinta2` en login, `--t`/`--paper`/`--line`/`--amber` en registro y cuenta, `--ink`/`--paper`/`--deep` en privacidad). `privacidad.html` además usa la familia de color "oscura" de `trial.css` (`#10253d`, `#f5f2e9`, `#ca8b2c`), no la de portada/resumen (`#12233b`, `#f7f6f2`, `#c8892a`) — una diferencia de valor real, no solo de nombre. Unificar esto requiere reescribir cada referencia `var(...)` dentro de 6 plantillas con estructura visual muy distinta entre sí (login es un layout partido, cuenta es un dashboard con barra lateral, privacidad es un documento legal), y decidir si las dos familias de color deben fundirse en una sola (cambio visual real) o mantenerse como dos tonos intencionales. No se tocó ninguno de estos 6 archivos en esta fase.

**Siguiente paso sugerido:** antes de seguir a Fase 3 (layout/ancho), decidir qué hacer con los dos puntos pendientes de arriba, ya que Fase 4 (tema claro/oscuro real) depende de tener resuelta la fuente de tokens por completo.

## Fase 3 — Layout y ancho (rama `fase-3-ancho`, sobre `fase-2-tokens`)

**Investigación previa (sin la cual esto se habría hecho mal):** antes de tocar `trial.css` investigué a fondo cómo funciona hoy el layout de `.articulo` con el JS activo, porque hay una cadena de 10 scripts que se parchean entre sí. Hallazgo clave: **el bug del §4 solo es visible cuando el JS no llega a ejecutar** (o durante el instante antes de que corra). Cuando el JS de trial sí corre (`future-experience.js`, función `enhanceTrial`), ya envuelve cada sección en `<section class="rt-evidence-section">`, ya arma un layout de grid de 2 columnas (`main.envoltorio{grid-template-columns:minmax(0,1fr) 300px}`), y ya inserta un `<aside class="rt-reader-rail">` en la columna derecha con "Tu progreso", "Hallazgo clave" y "En esta página" (una tabla de contenidos real, generada desde los `<h2>`) — es decir, **la idea de "índice + ficha + acciones en el lateral" que pide el prompt v3 ya existe para las páginas de trial**, construida en JS (`createReaderRail`, `future-experience.js`). Lo mismo existe, de forma más simple, para `resumen.html` (`makeRail` en `future-experience-fix-v4.js`). Por eso esta fase NO reconstruye un sidebar nuevo — ya está — y se concentra en arreglar el **estado base (sin JS)**, que es exactamente el que la Fase 1 acaba de dejar visible desde el primer render.

**Por qué no usé grid en `.articulo` (riesgo descartado):** mi primera idea fue convertir `.articulo` en `display:grid` con columnas para número+texto, igual que ya hacen los `<h2>`. La descarté al confirmar que `future-experience.js` (función `wrapTrialSections`) reestructura el DOM: mueve cada `<h2>` y sus `<p>` dentro de un `<section class="rt-evidence-section">` nuevo, así que los hijos directos de `.articulo` dejan de ser `h2`/`p` en cuanto el JS corre. Un grid con asignación de columna por hijo directo se habría roto en cuanto el JS se ejecutara (el caso más común). Por eso el arreglo usa selectores descendientes (`.articulo p`), que siguen funcionando sin importar cuántos envoltorios inserte el JS encima.

**Qué se cambió (`trial.css`, 4 puntos, todos dentro de las reglas de `.articulo p` / `.breve-cuerpo p`):**

1. Regla base: `margin:0 0 20px 96px` → `margin:0 auto 20px`. Antes el párrafo quedaba pegado a 96px del borde izquierdo con `max-width:76ch`, dejando el resto del contenedor de 1120px vacío a la derecha. Ahora el bloque de texto se centra dentro del contenedor. Deja de haber hueco muerto solo-a-la-derecha; el espacio sobrante (cuando lo hay) queda repartido en ambos lados, como una columna de lectura intencional.
2. Se quitó `.breve-cuerpo p{margin-left:96px}`, que reintroducía el mismo problema para la versión breve dentro de `<details>`.
3. En el breakpoint de 720px, se quitó `margin-left:48px` (ya no hace falta; el centrado no depende de esta corrección específica por ancho).
4. En el breakpoint de 420px, se quitó `margin-left:0` — con la regla base ya centrada, esta sobreescritura habría vuelto a fijar el texto a la izquierda en pantallas muy angostas.

**Efecto sobre el estado con JS activo:** ninguno. La capa "Future" ya neutraliza esta regla con `!important` (`future-experience.css:113` para trial, `future-experience-fix-v4.js:127-131` para `resumen.html`) sin importar lo que diga `trial.css`. El cambio de esta fase solo se nota cuando esas reglas `!important` no llegan a aplicarse — que es justo el escenario que la Fase 1 dejó de ocultar.

**Trade-off consciente, no oculto:** los encabezados `<h2>` mantienen su numeración alineada a 96px desde el margen izquierdo (con su propio grid interno, sin cambios). Los párrafos ahora se centran en vez de alinearse con ese mismo margen de 96px. En el estado base (sin JS) esto significa que el texto del párrafo ya no arranca exactamente debajo del texto del encabezado — es una compensación deliberada para no dejar espacio muerto; en el estado con JS (el habitual) esto no aplica porque el override ya resuelve el ancho de otra forma.

**Verificación:** sintaxis CSS validada con `tinycss2` (0 errores) sobre el archivo completo. No se pudo generar una captura de pantalla real: instalé Playwright y Chromium en este entorno, pero el sandbox no tiene permisos de administrador para instalar las dependencias del sistema que el navegador headless necesita (`sudo` está bloqueado). Recomiendo que quien revise este diff lo verifique visualmente en un navegador real antes de fusionar — es el primer cambio de las tres fases que sí modifica cómo se ve la página, a diferencia de Fase 1 y 2 que fueron verificables por computación exacta sin necesitar renderizado.

**No se tocó en esta fase:** `resumen.html` no tiene el bug — su estado base ya usa `columns:2` (multicolumna nativa de CSS), que no deja huecos fijos. El `enlace-original` (cita de la fuente original) tampoco tiene el bug: no le pusieron un `max-width` de lectura, así que ya usa todo el ancho disponible.

## Fases 4 y 5 — Tema claro/oscuro real, modo lectura, impresión unificada y limpieza de deuda (rama `fase-4-y-5-tema-lectura-pulido`, sobre `fase-3-ancho`)

**Nota importante sobre el pipeline (descubierta al publicar, no al diseñar):** entre que se generó el mapeo de Fase 0 y el momento de subir estas ramas al repositorio real, `main` avanzó 55 commits, incluyendo una consolidación real del pipeline: ahora existe `site-runtime.css` / `site-runtime.js` (generados por `node scripts/build-site-runtime.mjs` a partir de las mismas fuentes que veníamos editando: `future-experience.css`, `future-experience-patch.css`, `global-search.css`, `future-experience.js`, etc.) y cada página enlaza un único `<link>`/`<script>` a esos archivos en vez de la cadena de ~13 tags individuales que describía Fase 0. Esto es, literalmente, lo que el prompt v3 original asumía que ya existía ("nunca edites site-runtime.css, edita sus fuentes y recompila") — Fase 0 documentó correctamente que no existía *en ese momento*; ahora sí.

Consecuencia práctica para esta entrega: en vez de añadir `theme-light.css`, `reader-mode.css`, `print.css` y `reader-preferences.js` como tags nuevos en `aplicar_experiencia_futura.py` (el plan original, cuando ese archivo era el único inyector), se agregaron como **fuentes nuevas** en `scripts/build-site-runtime.mjs` (`cssSources`/`jsSources`) y se regeneró `site-runtime.css`/`site-runtime.js` con `node scripts/build-site-runtime.mjs`. Resultado: **ninguna de las páginas HTML generadas cambió** (verificado programáticamente: 0 de 177 páginas candidatas modificadas), porque el tag que cada página ya carga no cambió — solo cambió el contenido del archivo al que apunta. Esto también evitó por completo el conflicto de fusión que habría existido al reconciliar 61+ páginas regeneradas dos veces con inyectores distintos (el propio commit de `main` "Regenerar experiencia futura v4 con AdSense" tocó los mismos puntos de inyección).

El flujo de trabajo restante es exactamente el que describía Fase 0 en su §2: **editar fuentes, no los archivos generados.** `site-runtime.css`/`.js` en este commit ya están regenerados y listos; además, `future-regenerate.yml` los reconstruye automáticamente en cada push a `main` que toque estas fuentes, así que aunque no se hubieran regenerado a mano aquí, el propio pipeline lo habría hecho al fusionar.

### Fase 4a — Tema real (`future-experience.js`)

Se reemplazó el `document.body.classList.add('rt-future')` incondicional al inicio de `pageClass()` por una decisión real: `resolveTheme()` (nueva función) revisa `localStorage['rt-tema']` y, si no hay preferencia guardada, `prefers-color-scheme`. Las rutas de cuenta (`rt-future-account`: login/registro/recuperar/cuenta/biblioteca/privacidad/agregar) y las institucionales (`rt-future-institutional`: metodología/equipo-editorial, y ahora también `/privacidad/` y `/terminos/`, agregadas a `main` en el commit de OAuth) se quedan **fijas en oscuro** — no tienen variante clara todavía (ver "pendiente" abajo) — así que ahí no hay ningún cambio de comportamiento. En el resto de rutas (home, trial, resumen/legacy, hub, cluster) el tema ahora se decide de verdad y se puede alternar.

### Fase 4b — Tema claro real (`theme-light.css`, archivo nuevo)

~95 reglas con el patrón `:not(.rt-future) <selector-oscuro-original>{...!important}`, mapeadas a los tokens de la paleta clara consolidada en Fase 2 (`--tinta`, `--tinta-2`, `--teal-hondo`, `--papel`, `--papel-2`, `--linea`, `--ambar-texto`). El truco de especificidad: `:not(.clase)` cuenta como una clase real en la especificidad CSS, así que `:not(.rt-future) selector` siempre supera a `selector` a secas — no hace falta conocer el orden de las reglas oscuras originales para garantizar que esta capa gane cuando el tema es claro.

Cubre: topbar/nav, botón "Guardar", riel del lector completo (`.rt-rail-card`, progreso, hallazgos, fuentes), tarjetas de resumen, `.rt-evidence-section`, partes de portada/trial/hub/cluster/legacy (`resumen.html`). **No cubre** (documentado explícitamente, no un olvido): `.fila.rt-featured` y `.seo-hubs-home` (componentes oscuros autocontenidos con su propio look), botones con gradiente propio, y las rutas de cuenta/institucional (sin variante clara aún).

### Fase 4c — Modo lectura (`reader-mode.css`, `reader-preferences.js`, archivos nuevos)

Un botón de tema (sol/luna, con `aria-pressed`/`aria-label`) y, solo en trial/resumen, un botón de "Modo lectura" que oculta el riel lateral y la ficha de resumen y centra el artículo a `74ch`. `reader-preferences.js` es un IIFE aislado (no toca la lógica de `future-experience.js` más allá de leer `window.__rtTemaDisponible`, que Fase 4a ya expone) que espera con `requestAnimationFrame` (hasta 90 intentos) a que ese flag esté definido antes de decidir si inyecta los controles — evita una condición de carrera con la inicialización asíncrona existente. Persiste ambas preferencias en `localStorage` y sigue los cambios de tema del sistema operativo en vivo si el usuario no fijó una preferencia manual.

### Fase 4d — Verificación de contraste

Verificado por cálculo directo (luminancia relativa / ratio de contraste WCAG 2.x reimplementado en Python, sin navegador): todos los pares texto/fondo del tema claro nuevo cumplen AA, y el cuerpo de texto principal cumple AAA.

### Fase 5 — Impresión unificada (`print.css`, archivo nuevo)

Antes, `@media print` solo existía en `resumen.html` y ni ahí neutralizaba de verdad la capa oscura (sus reglas `!important` seguían ganando también al imprimir). `print.css` se carga en todas las páginas y actúa solo dentro de `@media print`: fondo blanco/texto negro forzado, oculta cromo (topbar, riel, botones de acción, ads, filtros) y decoraciones (glow, orbit), y iguala especificidad con `!important` + prefijo `html body.X` contra las reglas oscuras que también usan `!important`. Extiende impresión, por primera vez, a las 39 páginas de trial.

### Fase 5 — Limpieza de deuda real (no solo cosmética)

**Hallazgo con impacto funcional real:** `legacy-reader-design-v5.js`, inyectado dinámicamente por `pdf-contact.js` en cada carga de `resumen.html` (confirmado también en el `main` actual — no se corrigió solo con el paso del tiempo), redefine `--tinta`, `--papel`, `--teal`, etc. con **`!important` directamente sobre la declaración de la variable CSS**. Una variable con `!important` gana el cascade sin importar el orden de las hojas de estilo ni su especificidad — así que este script estaba corrompiendo en silencio, en cada visita a `resumen.html`, tanto la consolidación de tokens de Fase 2 como el interruptor de tema de Fase 4 en esa página específica. Se quitó su inyección de `pdf-contact.js` (se conservó `legacy-seo.js`, que solo lee tokens con `var()` y cumple una función real: URL canónica/robots/og:url). Se borraron `legacy-reader-design-v4.js` (confirmado sin referencias en ningún lado) y `legacy-reader-design-v5.js`, y se actualizaron `.github/workflows/future-experience.yml` y `future-regenerate.yml` para quitar el `node --check legacy-reader-design-v5.js` que habría fallado con el archivo borrado.

**Se dejó fuera a propósito (no por omisión):** el font-stack `--rt-font` de `future-experience.css` traía `'Inter'` primero; se confirmó (de nuevo, contra el `main` actual) que esa fuente nunca se enlaza desde Google Fonts en ninguna página — el stack real cae siempre al de sistema. Se quitó la referencia muerta.

### Resumen de lo que queda pendiente (sin tocar, por decisión explícita)

1. Tema claro para `rt-future-account` e `rt-future-institutional` (incluye ahora `/privacidad/` y `/terminos/`, rutas nuevas en `main`): siguen fijas en oscuro.
2. Los ~115 colores hex casi-duplicados (no idénticos) en `future-experience.css` se dejaron como literales — agruparlos causaría cambios de píxel imperceptibles pero no-cero que preferí no aplicar sin QA visual o aprobación explícita.
3. Fragmentación de nombres de variable entre las 6 páginas de cuenta (cada una con su propio esquema: `--tinta`/`--tinta2`, `--t`/`--paper`/`--line`/`--amber`, `--ink`/`--deep` en `privacidad.html`, que además usa los valores de la "Familia 2" de `trial.css`) — documentada, no tocada.
4. **Sigue sin haber verificación visual en navegador real** para Fases 3, 4 y 5: Playwright se instaló parcialmente en el entorno de generación (el binario de Chromium sí se descargó) pero no se pudieron instalar sus dependencias de sistema por falta de permisos de administrador (`sudo` bloqueado). El propio CI del repositorio (`future-experience.yml`) sí instala Playwright completo y corre capturas de auditoría visual — se recomienda revisar esas capturas en el PR antes de fusionar.
5. Breakpoints inconsistentes (700/720/760px, etc.) — de bajo riesgo, no se tocaron.

## Hotfix — corrección de bug real detectado por el CI tras publicar Fase 4/5

Al fusionar los 4 PRs, el workflow `future-experience.yml` (que sí corre Playwright completo en CI) detectó una regresión real: en páginas de trial con tema claro, los enlaces "Metodología" y "Equipo editorial" del menú principal, que deben verse solo en portada, reaparecían.

**Causa raíz:** `.rt-future` no es una clase exclusiva de "tema oscuro" — es la clase estructural que activa toda la capa "Future" (cientos de reglas de layout, no solo color). Docenas de reglas en toda la cadena de scripts dependen de que esté siempre presente una vez que el JS corre — incluida la que oculta esos enlaces fuera de portada (`future-experience-final.js`). El cambio original de Fase 4 quitaba `.rt-future` del body para representar "tema claro", rompiendo todas esas reglas no relacionadas con color.

**Corrección:** `.rt-future` vuelve a agregarse siempre (como antes de Fase 4). El tema claro ahora se representa con una clase adicional y separada, `rt-tema-claro`, que se agrega junto a (nunca en lugar de) `.rt-future`. Se actualizaron en consecuencia:
- `future-experience.js`: `pageClass()` ya no condiciona `.rt-future`; agrega `.rt-tema-claro` aparte cuando corresponde.
- `theme-light.css`: el gatillo `:not(.rt-future)` se reemplazó por `.rt-tema-claro` (misma especificidad, misma garantía de que gana siempre). De paso se corrigió un bug independiente y preexistente: varias reglas combinaban el gatillo con una clase de ruta (`rt-future-home`, `rt-future-trial`, `rt-future-hub`, `rt-future-cluster`, `rt-future-legacy`) con un espacio de por medio (combinador de descendiente) en vez de como selector compuesto — como ambas clases viven en el mismo `<body>`, esas reglas nunca podían coincidir con nada. Esto significa que la mayor parte del estilo claro específico de portada/trial/hubs/legacy nunca llegó a aplicarse la primera vez que se publicó esto, algo que solo un navegador real habría revelado.
- `reader-mode.css`: se invirtió qué bloque es el "default" sin calificar (ahora oscuro, porque `.rt-future` es permanente) y cuál es el override (`rt-tema-claro`, antes al revés).
- `reader-preferences.js`: el botón de tema ahora alterna `rt-tema-claro`, nunca `rt-future`.

Se verificó de nuevo que 0 páginas HTML candidatas cambiaron (el fix es solo en archivos fuente + `site-runtime.css`/`.js` regenerado). Sigue pendiente la verificación visual real en navegador — se recomienda encarecidamente revisar las capturas de Playwright de este PR antes de fusionar, dado que el bug anterior pasó dos rondas de revisión humana y verificación estática sin detectarse.

## Rediseño integral — Fase 1 (resumen.html: columna de lectura)

Respaldo de seguridad creado antes de empezar: rama `respaldo-pre-rediseno-20260906`, apuntando a `0dc5030` (estado de `main` justo antes de esta fase). Rollback exacto si algo se rompe:

```
git push origin respaldo-pre-rediseno-20260906:main --force
```

**Hallazgo de auditoría no documentado hasta ahora:** `legacy-unifier-v4.js` (bundleado en `site-runtime.js`) reescribe en vivo, solo en `resumen.html`, buena parte de `header.art`: reconstruye `.migas` como breadcrumb "RUTA · Volver al índice › Especialidad › Resumen completo/breve", inserta un eyebrow ("Ensayo clínico aleatorizado"), reclasifica las etiquetas con `SpecialtyClassification` + `temas`, añade la fecha de publicación, clona el enlace de `.version-nav` dentro de `.acciones-art` (y oculta `.version-nav` original), y aplica un grid de 6 filas a `header.art`. Esto ya cumple gran parte de lo que un "masthead" NEJM-like pide (ficha + acciones bien jerarquizadas). Por eso el intento inicial de esta fase — mover `.acciones-art`/`.version-nav` fuera de `header.art` a un aside lateral — se revirtió: rompía las suposiciones de `header.querySelector(...)` de ese script. Lección para las fases siguientes: **antes de tocar la estructura de cualquier página, greppear qué scripts del bundle hacen `querySelector`/`insertBefore`/`classList` sobre sus clases**, no solo leer el HTML/CSS estático.

**Cambio real (el único que hacía falta):** el cuerpo del artículo (`<article>`) usaba `columns:2` en pantalla ancha — dos columnas tipo revista, difíciles de seguir en texto clínico largo con reingreso ocular constante. Se reemplaza por una sola columna de ancho controlado (`max-width:74ch`), alineada a la izquierda en pantalla (el justificado con guiones automáticos se conserva únicamente para `@media print`, donde sí ayuda a ahorrar papel). No se tocó ninguna función JS (PDF, SEO, clasificación, relacionados, saneado de HTML): el cambio es 100% CSS. Se retiró además un `<script src="/site-runtime.js">` duplicado (dos query-strings de versión distintas cargando el mismo bundle dos veces).

**Segundo hallazgo, corregido como red de seguridad:** `future-experience.css` fuerza `background`/`color` oscuros en `body.rt-future` de forma incondicional (`!important`), y nunca existía el contrario para `body.rt-future.rt-tema-claro`. En `resumen.html` esto no se notaba porque `legacy-unifier-v4.js` agrega su propia clase (`rt-legacy-normalized`) y el fondo correcto ya se ve bien en la práctica (verificado en navegador real, claro y oscuro). Pero no hay garantía de que exista un normalizador equivalente en cualquier otra página "legacy" futura, así que se agregó `body.rt-future.rt-tema-claro{background:var(--papel);color:var(--tinta)}` en `theme-light.css` como red de seguridad documentada — gana por especificidad (2 clases) sin depender del orden de carga.

**Verificación (navegador real, no solo estática):** publicado directo a `main` (commit `1114eef`), verificado por MD5 vía Contents API contra el blob de `main` (coincide exacto), CI 100% verde (`build`, `audit`, `production-health`, `download-contract-smoke`, `data-validation`, `browser-smoke`, `future-experience`, `future-production`, `deploy`), y comprobado en vivo con un navegador real sobre `resumenestrials.com/resumen.html?id=98`: columna única, 4 botones de PDF intactos, toggle claro/oscuro funcionando en ambos sentidos, fondo claro correcto.

**Pendiente de este mismo rediseño integral (continúa en fases siguientes, no se abandona):** sistema de tokens consolidado, reconstrucción de `index.html` (masthead/retícula/filtros) y `agregar.html`, armonización visual de `login.html`/`registro.html`/`recuperar.html`/`cuenta.html`/`biblioteca.html` (sin tocar `auth.js`/Supabase/Turnstile), y pruebas exhaustivas finales. Ver tareas abiertas.

## Rediseño integral — hallazgo y fix menor en la portada

Auditando `index.html` antes de decidir cómo tocarlo, aparecieron **6 scripts más** que no estaban en el inventario original porque no pasan por `scripts/build-site-runtime.mjs`: `home-auth-ui.js`, `interactive-home.js`, `library-filter-cleanup.js`, `recommendations.js`, `home-visual-tuning.js`, `home-control-layout.js` y `seo-hubs-home.js` se cargan con `<script>` directamente desde `index.html`, en paralelo al bundle `site-runtime.js`. Entre ellos, `home-visual-tuning.js` resultó ser un rediseño completo (y ya vigente) de la retícula de la portada — numeración por `counter()`, tipografía Fraunces para los años, grid propio — que **superpone y en la práctica reemplaza** buena parte del bloque `.rt-future-home` que sigue viviendo en `future-experience.css` desde una fase anterior.

Un resto de ese bloque superado seguía vivo: `.rt-future-home .anio-margen::after{content:"/ ARCHIVO"}` y el `.anio-num::after{content:" / archivo"}` de `home-visual-tuning.js` apuntan a dos elementos anidados distintos (`.anio-margen` es el padre de `.anio-num`), así que ambos pseudo-elementos se pintaban a la vez: la etiqueta "/ ARCHIVO" aparecía duplicada, una vez grande junto al año y otra vez chica debajo. Se eliminó la declaración muerta de `future-experience.css` (nada más dependía de ella, verificado por grep). Publicado en `main` (commit `f2d5253`), CI verde, confirmado por `curl` directo contra `resumenestrials.com/site-runtime.css` que el archivo servido ya no contiene la regla — la comprobación visual en el navegador de la sesión quedó bloqueada por una caché de disco propia de esa herramienta que no se logró invalidar (no representativa de un visitante real, que sí recibe el archivo corregido en cuanto expira el `max-age=600` de por sí).

**Conclusión que cambia el plan para `index.html`:** la portada ya tiene una retícula editorial trabajada (masthead con watermark del logo, contadores en vivo, ficha/año con numeración tipográfica, filtros, tarjeta destacada oscura) construida a través de al menos 17 scripts que se reparten responsabilidades muy específicas (autenticación, favoritos/biblioteca, recomendaciones, clasificación, descargas, ajustes visuales, SEO). Reescribir `index.html`/`_includes/index-source.html` desde cero, como pedía el prompt original, obligaría a re-verificar la compatibilidad con los 17 a la vez — el mismo riesgo que ya se evitó a tiempo en `resumen.html` con `legacy-unifier-v4.js`, pero multiplicado. El camino seguro y consistente con lo ya aprendido es seguir el mismo patrón: auditar en vivo, encontrar deuda real y verificable, corregirla puntualmente, publicar y verificar cada pieza — no una reescritura total sin red de seguridad visual.

## Rediseño integral — login/registro/cuenta ya rediseñadas; token consolidado en las 5 páginas de cuenta

Auditando `agregar.html` para decidir cómo reconstruirla, apareció el mismo patrón que ya se vio en `resumen.html` e `index.html`: `agregar-editorial-dates.js` ya la rediseña por completo en vivo (estética "MESA DE EDICIÓN"), y las 5 páginas de cuenta (`login.html`, `registro.html`, `recuperar.html`, `cuenta.html`, `biblioteca.html`) tienen su propio rediseño claro y completo ya vigente vía `member-design-v3.js`, no documentado hasta ahora. Ninguna de las dos necesita reconstruirse desde cero.

**Bug real encontrado y corregido (commit `36e118a`):** `.rt-future-account .shell` traía un fondo oscuro (`linear-gradient` sobre azul marino) de una fase de diseño anterior a `member-design-v3.js`. Como éste no toca el fondo de `.shell` (solo borde y alto mínimo), y el texto de `.story p`/`.trust`/`.sub`/`.alta` tiene la misma especificidad en ambas hojas con `member-design-v3.js` ganando por cargarse después, el resultado era texto oscuro (`#496078`, pensado para fondo claro) sobre una tarjeta oscura heredada — casi ilegible. Confirmado en vivo en `login.html` antes y después del fix. Afecta también a `registro.html` y `cuenta.html` (las tres usan `.shell`); `recuperar.html` y `biblioteca.html` no lo usan y no estaban afectadas. Se retiró únicamente la declaración `background` de esa regla.

**Consolidación de tokens (commit `7ff51fb`):** las 5 páginas de cuenta declaraban la misma paleta compartida (valores hex idénticos entre sí y con `future-experience.css`) bajo tres esquemas de nombres distintos (`--tinta2/--papel2/--teal2` en login; `--t/--t2/--paper/--paper2/--line/--amber/--teal2` en registro/cuenta/biblioteca; `recuperar.html` ya usaba los nombres compartidos). Se renombró todo al esquema único (`--tinta`, `--tinta-2`, `--teal`, `--teal-hondo`, `--papel`, `--papel-2`, `--linea`, `--ambar`) y se retiró la redeclaración local, dejando en cada `:root` solo lo específico de esa página (`--err`/`--ok`). Cambio de nombres puro, cero cambio de valores — verificado en vivo que las 5 páginas se ven exactamente igual antes y después.

Con esto, la fragmentación de nombres de variables documentada como pendiente en la Fase 2 original queda resuelta para las 5 páginas de cuenta. Sigue pendiente (menor, cosmético, no verificado como bug real): `agregar-editorial-dates.js` parece tener partes de su propio CSS superadas por reglas más específicas de `future-experience.css` para esa ruta (`.rt-future-account`), de forma similar a lo de `.shell` — no se tocó porque no se confirmó un problema de contraste o legibilidad concreto ahí, solo una posible redundancia.

## Rediseño integral — bug funcional real en el filtro de año de la portada

Probando a fondo el buscador y los filtros de `index.html` (no solo su aspecto) apareció un bug funcional, no solo cosmético: `interactive-home.js` calculaba el año de cada trial con `(r.fecha || '').slice(0,4)`, asumiendo que `fecha` siempre viene en formato ISO (`AAAA-MM-DD`). 11 de los 150 registros de `resumenes.json` (SPRINT, SMART, SALT-ED, PLUS, BOX, INTERACT4, BPROAD, LOVIT, HOT-ICU, CREST-2, TAME) tienen `fecha` en texto en español ("27 de agosto de 2022", en un caso con una fecha de actualización entre paréntesis) — `slice(0,4)` devolvía fragmentos como "27 d" o "9 de". Efecto doble: el selector de año de la portada mostraba 6 opciones basura mezcladas con los años reales, y al filtrar por un año donde SÍ caía uno de estos 11 trials (por ejemplo 2022 para BOX o LOVIT), el trial quedaba excluido por error — invisible para cualquiera que buscara evidencia de ese año.

Por la restricción explícita de no tocar `resumenes.json`, se corrigió únicamente la extracción en `interactive-home.js`: en vez de asumir posición fija, se toma el primer bloque de 4 dígitos de la cadena (`/\d{4}/`), que es el año tanto en formato ISO como en el texto en español. Publicado (commit `ec827bd`), CI verde, verificado en vivo: el selector ya solo lista años reales y BOX/LOVIT aparecen correctamente al filtrar por 2022.

Esto confirma el valor de probar la *funcionalidad* de cada pieza (no solo su aspecto) antes de dar por buena una página ya "rediseñada" por scripts anteriores.

## Fix: grupos de año vacíos quedaban visibles al filtrar (bloques en blanco en el índice)

**Síntoma:** al filtrar el índice de la portada por año (selector "Todos los años" → un año concreto), los años sin resultados no desaparecían del todo: el rótulo del año y el bloque contenedor seguían ocupando ~150-160px de espacio en blanco cada uno, en vez de colapsarse. Con 17 años en el selector y solo 1-2 con resultados tras filtrar, esto producía varias pantallas de espacio vacío entre los resultados reales.

**Causa raíz:** `interactive-home.js` (`applyPersonalFilters()`) oculta cada `.grupo-anio` sin filas visibles asignando `g.style.display = 'none'` (estilo inline). Pero tanto `future-experience.css` (`.rt-future-home .grupo-anio{display:grid!important;...}`) como `home-visual-tuning.js` (que inyecta la misma regla `.grupo-anio{display:grid!important;...}` sin el prefijo `.rt-future-home`) marcan esa propiedad como `!important`. Una regla de hoja de estilos `!important` siempre gana sobre un estilo inline no-`!important`, así que el `display:none` puesto por JS nunca llegaba a aplicarse: el grupo permanecía com `display:grid` pese a que todas sus filas internas sí estaban correctamente ocultas.

**Fix:** en `interactive-home.js`, el grupo vacío ahora se marca con `g.classList.toggle('rt-grupo-vacio', !visible)` en vez de tocar `style.display`. En `future-experience.css` se añadió `.rt-future-home .grupo-anio.rt-grupo-vacio{display:none!important}`, cuya especificidad (3 clases) supera tanto a la regla de 2 clases de `future-experience.css` como a la de 1 clase de `home-visual-tuning.js`, independientemente del orden de carga.

**Verificación:** en `resumenestrials.com`, filtrar por año 2022 antes del fix dejaba ~16 bloques vacíos (uno por cada año sin resultados) intercalados con los 8 ensayos reales de 2022. Tras el fix y con `node scripts/build-site-runtime.mjs` regenerado, solo el bloque de 2022 permanece visible.

## Ajuste al fix anterior: reubicar la regla `.rt-grupo-vacio` al final de la hoja

El fix de "grupos de año vacíos visibles al filtrar" (commit anterior) insertaba la regla `.rt-future-home .grupo-anio.rt-grupo-vacio{display:none!important}` en línea, justo después de `.rt-future-home .grupo-anio{...}` dentro del bloque minificado de una sola línea de la sección "índice" de `future-experience.css`. Al verificar en vivo, el motor CSS del navegador descartaba silenciosamente esa regla en esa posición exacta (confirmado con `CSSStyleSheet.insertRule`, que la acepta sin error en cualquier otra posición). Se reubicó la misma regla, sin cambios, al final de `future-experience.css` (después del `@media(prefers-reduced-motion:reduce)` final), donde se parsea y aplica con normalidad. La especificidad de 3 clases no depende de la posición en el archivo, así que el fix funciona igual.

## Rediseño integral — 5 hallazgos reportados tras revisión visual (capturas del usuario)

El usuario revisó la portada y `resumen.html` en vivo (5 capturas) y reportó 6 puntos concretos. Se investigó y corrigió cada uno; los 5 primeros se publicaron juntos (commits `9735af9`, `9fa1323`, `6b83243`, `998a3df`), el sexto (contraste de `resumen.html` en modo claro) se documenta aparte más abajo porque fue el más laborioso de diagnosticar.

**1. Conteo "150" vs "73+84" en la portada:** no era un bug de cálculo — 73 (Medicina Crítica) + 84 (Medicina Interna) = 157 porque 7 ensayos pertenecen a ambas especialidades a la vez (p. ej. cardiología crítica, clasificada como interna por especialidad principal y como crítica por secundaria), y `perteneceA()` los cuenta en las dos. El total real (150) ya era correcto. Se añadió una nota aclaratoria (`<p class="meta-nota">`) dentro de `.barra-meta` en `_includes/index-source.html`, con su CSS a juego (`future-experience.css`), explicando el porqué en vez de "arreglar" un número que ya estaba bien.

**2. Hero de portada confinado a la izquierda:** el ancho estaba limitado a 1380px por una regla inyectada en runtime por `future-experience-final.js` (`body.rt-future-home header.sitio .envoltorio{max-width:1380px!important}`). **Primer intento fallido:** quitar el límite (`max-width:none`) y además forzar `header.sitio` a una sola columna de grid (creyendo que la segunda columna de `minmax(270px,.58fr)` de `home-visual-tuning.js` era espacio muerto sin usar). Ambos cambios rompieron `scripts/future-experience-smoke.mjs`, que exige que `header.sitio .envoltorio` quede centrado en un viewport de 1440px (margen >20px a cada lado) — sin el límite, el hero queda pegado al borde izquierdo sin margen. **Fix definitivo:** `max-width:clamp(1380px,82vw,1800px)`, que da exactamente 1380px (idéntico a antes) en cualquier viewport ≤~1683px — la prueba automatizada pasa sin cambios de geometría — y crece hasta 1800px en monitores anchos (1920px, 2560px), donde sí se veía angosto.

**3 y 4. Widget "Tu progreso" ilegible en modo claro y ovalado en modo oscuro:** dos bugs reales en `.rt-progress-ring` (el círculo de progreso de lectura, compartido por `/trials/<slug>/` y `resumen.html`). (a) Forma ovalada: el anillo vive en una fila `display:flex` (`.rt-progress-row`) sin `flex-shrink:0`, así que en raíles angostos el contenedor comprime su ANCHO pero no su alto — se añadió `flex-shrink:0;flex-grow:0;min-width/height:58px` en `future-experience.css`. (b) Invisible en modo claro: `theme-light.css` nunca sobreescribía el fondo del arco (`conic-gradient`) ni el color del texto "0%" del propio `.rt-progress-ring`, solo el agujero central y el track — se añadieron ambas reglas. Complicación adicional descubierta en el camino: en `resumen.html` (no en `/trials/`), `future-experience-fix-v4-compat.js` inyecta sus propias reglas `html body.rt-future.rt-future-legacy .rt-progress-ring/...-value/...-track{...!important}` con mayor especificidad que las de `theme-light.css`, así que las correcciones de modo claro para esa página se repitieron con selectores de especificidad más alta (`html body.rt-tema-claro.rt-future.rt-future-legacy ...`) para garantizar que ganen siempre, sin depender del orden de inserción en el DOM.

**5. "Intercambio de formatos" entre la página nueva y la anterior:** son dos implementaciones independientes del mismo widget (`future-experience.js` para `/trials/`, con `<strong>0%</strong>`; `future-experience-fix-v4.js` para `resumen.html`, con `<span class="rt-progress-value">0%</span>`), con huecos de cobertura de CSS distintos. Las correcciones del punto 3/4 cubren ambas implementaciones explícitamente (selectores separados para `.rt-progress-ring strong` y `.rt-progress-value`), así que visualmente ambas páginas quedan consistentes entre sí; no se unificó el código JS duplicado por ser un cambio de mayor riesgo sin beneficio visual adicional.

**Verificación:** los 5 puntos se comprobaron en vivo reconstruyendo a mano, vía `javascript_exec`, el marcado y las clases exactas que el propio `init()` del sitio produce (el navegador de esta sesión permanece con `document.hidden=true`, lo que pausa el `requestAnimationFrame` del que depende `init()` — limitación del entorno de verificación, no del sitio: en una pestaña visible normal corre en el siguiente frame). El punto 2 (hero) se verificó además con Playwright real (Chromium headless-shell, con las librerías de sistema que faltaban extraídas manualmente de paquetes `.deb` sin necesitar privilegios de root) corriendo las 8 pruebas smoke relevantes del repo antes de publicar: `future-experience-smoke`, `library-filter-smoke`, `future-final-smoke`, `unified-reader-v4-smoke`, `reader-controls-v5-smoke`, `browser-smoke` (estricto), `download-contract-smoke` (PDF completo y breve) y `home-controls-smoke` — las 8 en PASS. CI 100% verde en el commit final (`998a3df`). MD5 verificado vía Contents API contra los 4 archivos tocados.

## Fix: contraste de texto en modo claro en `resumen.html` (anomalía de especificidad CSS)

**Síntoma:** en la vista breve de `resumen.html` con tema claro activo, el texto del cuerpo del artículo (`article p`) se veía en un gris azulado pálido (`rgb(203,215,219)`) sobre fondo crema (`rgb(247,246,242)`), con muy poco contraste — casi ilegible, confirmado en la captura del usuario.

**Diagnóstico (el más largo de esta ronda):** `theme-light.css` ya tenía `.rt-tema-claro.rt-future-legacy article p{color:var(--tinta-2)!important}`, con especificidad (0 IDs, 2 clases, 2 elementos) — en teoría más específica que cualquier regla razonable de tema oscuro. Sin embargo, en el navegador seguía ganando el color pálido. Se probó, en este orden, hasta encontrar la causa real: (1) confirmar que el selector claro realmente hace `match()` sobre el `<p>` real — sí; (2) inspeccionar todas las hojas de estilo cargadas y sus reglas con `color` que hacen match sobre ese `<p>`, calculando la especificidad real de cada una, no de memoria. Ahí apareció la regla ganadora real, inyectada en runtime por `legacy-unifier-v4.js`: `body.rt-future-legacy article.corto p{color:rgb(203,215,219)!important}` — especificidad (0,2,3): **3 elementos** (`body`, `article`, `p`) más 2 clases (`rt-future-legacy`, `corto`), que **supera** a la regla de `theme-light.css` (0,2,2: solo 2 elementos, `article`+`p`) precisamente por el elemento `body` adicional. No era un bug del navegador ni una anomalía de la cascada CSS — fue un error de cálculo de especificidad en el fix anterior: se comparó de memoria en vez de contar elemento por elemento.

**Fix:** en `theme-light.css`, las dos reglas de color de `article p`/`article li` para `rt-future-legacy` pasan a `html body.rt-tema-claro.rt-future-legacy article p, ...article li, ...article.corto p, ...article.corto li{color:var(--tinta-2)!important}` — con `html`+`body` añadidos, la especificidad (0,2,4 o 0,3,4 según la variante) supera siempre a la de `legacy-unifier-v4.js`, sin importar el orden de inserción en el DOM. Mismo patrón aplicado también a las reglas del widget "Tu progreso" para esa misma página (ver hallazgo 3/4 arriba), una vez confirmado que el mismo tipo de error se repetía ahí.

**Verificación:** en vivo, con las clases reales del body (`rt-tema-claro rt-future-legacy rt-future`), el color computado de `article p` pasa de `rgb(203,215,219)` a `rgb(56,80,110)` (equivalente a `--tinta-2`), con buen contraste sobre el fondo crema `rgb(247,246,242)`. Publicado junto con el resto de esta ronda; CI verde; MD5 verificado.

## Fix: raíl "Tu progreso" tapado por el topbar al hacer scroll (offset sticky fijo vs. topbar de altura variable)

**Síntoma:** en `/trials/<slug>/` (y `resumen.html`), al bajar la página el raíl lateral sticky ("Tu progreso", con el anillo de porcentaje y el índice "EN ESTA PÁGINA") aparecía cortado/tapado en la parte superior — el título "Tu progreso" no se veía, con el botón de descarga y el anillo empezando muy pegados al borde superior de la ventana.

**Diagnóstico (Playwright real, no el navegador oculto):** `.rt-reader-rail{position:sticky;top:94px}` asume que `.topbar` mide como máximo 94px de alto. Pero `.topbar` es TAMBIÉN sticky (`z-index:100`, por encima del raíl) y su altura real **varía entre 123px y 190px** según el ancho de viewport, porque `.rt-main-nav` pasa a una segunda fila en anchos intermedios (confirmado con capturas y `getBoundingClientRect()` en 1920/1440/1280/1100px — 190px es el caso en los dos anchos de laptop más comunes). Con `document.elementFromPoint()` en las coordenadas exactas del `<h3>Tu progreso</h3>` se confirmó que el elemento realmente pintado ahí era `.topbar-in` (el topbar), no el título del raíl: el raíl queda literalmente debajo del topbar, no solo visualmente apretado.

**Fix:** en vez de un número fijo, se mide la altura REAL del topbar en tiempo de ejecución. Nuevo archivo `topbar-offset-fix.js` (agregado a `jsSources` en `scripts/build-site-runtime.mjs`, así que se carga en todas las páginas vía `site-runtime.js`): mide `.topbar` con `getBoundingClientRect()` al cargar, al cambiar tamaño de ventana, cuando terminan de cargar las fuentes y mediante un `ResizeObserver` sobre el propio topbar, y expone el resultado como la variable CSS `--rt-topbar-h` en `:root`. No hace nada si la página no tiene `.topbar` (páginas sin ese componente quedan intactas). Los offsets `top:Npx` fijos que dependían de la altura del topbar pasan a `top:calc(var(--rt-topbar-h, Npx) + separación)`, con el valor original como respaldo si la variable aún no se calculó:
- `.rt-reader-rail` (raíl de progreso, `future-experience.css`) y su variante `body.rt-future-legacy .rt-reader-rail` (`future-experience-fix-v4.js`).
- `.anio-margen` e `.indice-cabecera` (índice de años y barra de filtros sticky de la portada, `home-visual-tuning.js`) — mismo patrón de bug, encontrado al auditar todo el código en busca de otros `position:sticky` con offset fijo.
- `.rt-member-biblioteca .tools` (barra de herramientas sticky de "Mi biblioteca", `member-design-v3.js`), que con `top:0` quedaba completamente oculta detrás del topbar en vez de solo apretada.

**Verificación:** Playwright real (Chromium headless-shell) en 4 anchos de viewport (1920/1440/1280/1100px) sobre una copia estática local del sitio (servidor `http.server` local, mismo repo). Para cada ancho se confirmó `--rt-topbar-h` calculado correctamente y, con `document.elementFromPoint()` en las coordenadas del `<h3>Tu progreso</h3>`, que el elemento realmente pintado ahí es ahora el propio `<h3>` (antes era el topbar) — en los 4 anchos. Lo mismo para `.anio-margen`/`.indice-cabecera` de la portada, sin cobertura del topbar en ningún ancho. Las 8 pruebas smoke del repo (`future-experience-smoke`, `library-filter-smoke`, `future-final-smoke`, `unified-reader-v4-smoke`, `reader-controls-v5-smoke`, `browser-smoke`, `download-contract-smoke`, `home-controls-smoke`) siguen en PASS tras el cambio.

## Fix: paneles de login/registro "fantasma" (texto casi invisible, no un problema de posición)

**Síntoma:** en `login.html`/`registro.html`, el panel izquierdo ("Evidencia sin ruido.") y el título del panel derecho ("Bienvenido de nuevo") se veían muy pálidos/traslúcidos, dando la impresión de dos paneles superpuestos o "fantasma".

**Diagnóstico:** no era un problema de posicionamiento (el grid de dos columnas de `.shell` está correctamente separado, sin overlap real, verificado con `getBoundingClientRect()`). El problema es de **contraste de color**, mismo patrón que el fix anterior en `resumen.html`: `future-experience.css` define un bloque completo `.rt-future-account{...}` (fondo oscuro degradado, texto color marfil/gris claro) pensado para un tema oscuro antiguo de las páginas de acceso, que ya no se usa (el fondo real, definido por `member-design-v3.js`, es claro/crema) pero cuya clase (`rt-future-account`) sigue presente en el `<body>` de todas estas páginas. Sus reglas de color, con selectores de 2 clases (p. ej. `.rt-future-account .story h1{color:var(--rt-ivory)}`, un marfil casi blanco `#f4f0e6`), ganaban por especificidad a las reglas de una sola clase de `member-design-v3.js`, dejando títulos (`.story h1`, `.formside h2`, `h1` genérico) y texto (`.story p`, `.trust`, `.alta`, `.lead`, `.meta`, y enlaces `.alta a`/`.meta a`) en un marfil o gris azulado casi ilegible sobre el fondo crema — visualmente indistinguible de un "fantasma" o dos capas mezcladas.

**Fix:** en `member-design-v3.js`, se añadieron reglas con selectores `html[data-rt-member-design="v4"] ...` (que ya se usa para marcar que el rediseño claro está activo) para las mismas propiedades, con especificidad garantizada por encima de `.rt-future-account` en todos los casos (atributo + clase + elemento siempre supera a 2 clases): títulos a `#10253d` (marfil→azul marino oscuro), texto de cuerpo a `#496078`, enlaces a `#0d5f61` (el teal oscuro ya usado en el resto del sistema v4).

**Verificación:** Playwright real, contraste calculado programáticamente (fórmula WCAG de luminancia relativa) contra el fondo real de cada página. Antes del fix, `.story h1`/`.formside h2` habrían dado ~1.1:1 (marfil `#f4f0e6` sobre crema `#f5f2e9`, prácticamente invisible). Después del fix: títulos 13.85:1, párrafos 5.81:1, enlaces 6.64:1 — todos por encima del mínimo WCAG AA (4.5:1 texto normal, 3:1 texto grande). Verificado en `login.html`, `registro.html` y `recuperar.html`. Captura de pantalla real confirma dos paneles nítidos y completamente legibles, sin ningún efecto de superposición. Las 8 pruebas smoke del repo siguen en PASS.

## Fix: NEJM aparecía 4 veces en el selector "Filtrar por revista"

**Síntoma:** el desplegable de revistas en la portada mostraba "NEJM", "NEJM Evidence", "New England Journal of Medicine" y "The New England Journal of Medicine" como 4 entradas separadas.

**Diagnóstico:** no es un bug de deduplicación — es que `resumenes.json` guarda el nombre de la revista tal como aparece en la cita original de cada artículo, y la misma revista (NEJM) fue citada de 3 formas distintas en las fuentes: 105 artículos como "NEJM", 13 como "New England Journal of Medicine", 11 como "The New England Journal of Medicine" (129 en total). "NEJM Evidence" sí es una revista hermana genuinamente distinta y no debía fusionarse. Por la restricción de no modificar `resumenes.json`, la normalización no puede vivir en el dato de origen.

**Fix:** en `interactive-home.js`, una función `normalizarRevista()` mapea las dos variantes largas a "NEJM" antes de (a) construir la lista única de opciones del desplegable y (b) evaluar si un artículo coincide con la revista seleccionada. Sin el paso (b), arreglar solo la lista visible habría dejado el filtro roto en silencio: elegir "NEJM" solo habría encontrado los 105 artículos con esa grafía exacta, ocultando los otros 24 sin ningún aviso — se corrigieron ambos lugares a la vez para que el filtro siga siendo correcto.

**Verificación:** Playwright real — el desplegable pasa de 7 a 6 opciones (incluyendo "Todas las revistas"), y seleccionar "NEJM" muestra exactamente 129 artículos de 150. Se actualizó también `scripts/library-filter-smoke.mjs`, que calculaba su propio resultado esperado con comparación de cadena exacta (el mismo bug, sin detectar, en la prueba) — ahora usa la misma normalización para calcular qué artículos deberían verse. Las 8 pruebas smoke del repo en PASS.

## Fix: tarjeta con esquinas redondeadas y sombra pesada en login/registro (leftover del tema oscuro)

**Síntoma:** tras el fix de contraste de la ronda anterior, el usuario reportó que login/registro "seguían estando estéticamente mal". El texto ya era legible, pero el panel de dos columnas (`.shell`) tenía esquinas redondeadas (18px) y una sombra oscura de gran difuminado (90px) — un tratamiento de "tarjeta flotante" fuera de lugar en un diseño por lo demás plano, de líneas finas, sin tarjetas ni sombras en ningún otro elemento de la página.

**Diagnóstico:** mismo origen que el fix de contraste anterior — `.rt-future-account .shell{border:1px solid var(--rt-line)!important;border-radius:18px!important;box-shadow:0 28px 90px rgba(0,0,0,.18)!important}` en `future-experience.css`, leftover del tema oscuro heredado que member-design-v3.js nunca neutralizó para estas propiedades (solo se habían corregido colores de texto). Confirmado con capturas ampliadas de las esquinas del `.shell`: borde de 1px visible en 3 lados con `--rt-line` (un color pensado para fondo oscuro, casi lavado sobre el crema), radio de 18px y sombra suave visibles en las 4 esquinas.

**Fix:** en `member-design-v3.js`, la regla `html[data-rt-member-design="v4"] .shell{...}` (que ya existía, solo zeraba `border-bottom`) se amplía para anular también `border-top/left/right`, `border-radius` y `box-shadow`, con especificidad garantizada por encima de `.rt-future-account` (atributo+clase supera a 2 clases). El único borde visible que debe quedar es el divisor vertical entre paneles (`.story{border-right:1px solid...}`), que ya funcionaba correctamente y no se tocó.

**Verificación:** Playwright real, capturas ampliadas antes/después de la esquina superior izquierda y del borde inferior de `.shell` en `login.html` y `registro.html` — confirmado visualmente que las esquinas ahora son rectas, sin sombra, consistentes con el resto del sitio. `getComputedStyle` confirma `border-radius:0px` y `box-shadow:none` en ambas páginas. Las 8 pruebas smoke del repo en PASS.

## Rediseño estético mayor: legibilidad, centrado y jerarquía (a pedido explícito del usuario)

**Contexto:** tras publicarse los fixes de la ronda anterior (NEJM×4→1, esquinas de `.shell`), el usuario reportó con capturas que el problema de fondo seguía sin resolverse: letras demasiado pequeñas en todo el sitio, NEJM Evidence todavía sin fusionar con NEJM, el hero de portada pegado a la izquierda, títulos de ensayo alineados a la izquierda, demasiadas etiquetas de clasificación sobre cada trial, y una barra superior con el buscador apretado entre el toggle de tema, el botón de cuenta y los íconos sociales. Se abordaron los seis puntos en una sola ronda, verificados con Playwright real (Chromium local, no el navegador embebido) antes de publicar.

**1. NEJM Evidence fusionado con NEJM.** El fix anterior fusionaba las 3 variantes de cita de NEJM pero dejaba "NEJM Evidence" como revista distinta. El usuario corrigió explícitamente: para este sitio es una sola revista. Se añadió `'NEJM Evidence': 'NEJM'` a `RT_ALIAS_REVISTA` en `interactive-home.js` y en la copia de la prueba (`scripts/library-filter-smoke.mjs`), con el comentario actualizado para dejar constancia de que la fusión es una decisión editorial explícita del sitio, no una afirmación de que ambas publicaciones sean la misma en la realidad.

**2. Hero de portada centrado.** `header.sitio .envoltorio` tenía `max-width` pero sin `margin` automático, así que a resoluciones anchas (1920px) el bloque quedaba pegado al borde izquierdo con ~625px vacíos a la derecha — confirmado con Playwright (`marginLeft:"0px"`, `marginRight:"0px"` antes del fix). Se añadió `margin-left:auto;margin-right:auto` a la regla existente en `future-experience.css`. Verificado tras el fix: márgenes izquierdo/derecho iguales (172.8px cada uno a 1920px de ancho).

**3. Títulos de ensayo centrados.** En `/trials/*/`, `.art-head` usaba una grilla de 2 columnas (`1fr 360px`) donde la segunda columna solo alojaba un círculo decorativo, dejando el título y las etiquetas empujados hacia la mitad izquierda con un `max-width` de 17ch en el `h1`. Se añadió un bloque a `future-experience.css` que convierte `.art-head` en una sola columna centrada (`grid-template-columns:1fr;max-width:920px;margin:0 auto;justify-items:center;text-align:center`), con el `h1` ampliado a `max-width:22ch`. Verificado: `h1` centrado dentro de la columna de 920px, `text-align:center` confirmado por cómputo de estilos.

**4. Etiquetas de clasificación reducidas a especialidad + subespecialidad.** Las páginas de trial mostraban hasta 4 etiquetas (especialidad, subespecialidad y 2+ temas/palabras clave sueltos). El usuario pidió dejar solo la especialidad principal (Medicina Interna o Medicina Crítica) y, si aplica, la subespecialidad. Se ocultan con `.rt-future-trial .badges .tema:not(.tema-link){display:none!important}` — los plain `.tema` (temas/palabras clave) desaparecen; se conservan `.tema.tema-link` (subespecialidad, puede haber más de una) y `.badge` (especialidad principal, siempre exactamente una). Verificado en un trial con 5 etiquetas originales: quedan visibles solo 2 (subespecialidad + especialidad), las otras 3 confirmadas `display:none` por cómputo de estilos. Nota: `resumen.html` (formato legado) y las páginas hub/cluster (listados por subespecialidad) usan sus propias funciones/markup y ya mostraban solo lo pertinente — no requerían este cambio.

**5. Tamaños de fuente aumentados en textos pequeños, sitio-wide.** Se subieron ~45 declaraciones `font-size`/`font` de 8-10px a 11-13px en `future-experience.css`, `home-visual-tuning.js`, `member-design-v3.js`, `future-experience-final.js`, `agregar-editorial-dates.js` y `reader-mode.css` (migas de pan, etiquetas, controles de preferencia, metadatos). **Hallazgo importante durante la verificación:** varias de estas mismas propiedades ya estaban sobreescritas a un tamaño mayor (14-16px) por capas de especificidad más alta añadidas en rondas anteriores (`future-experience-fix-v4.js` y el script cargado dinámicamente `future-experience-fix-v4-compat-base.js`), así que el aumento de base es principalmente una red de seguridad para cuando esas capas no aplican (p. ej. `document.hidden` en pruebas automatizadas) — no un cambio visual duplicado para el usuario real. Se encontró y corrigió un caso genuinamente no cubierto: `.tema` en las páginas hub/cluster (listados por subespecialidad, ej. `/medicina-interna/neumologia/`) seguía en 9px porque la única regla que lo alcanzaba era la base sin calificar de `trial.css`; se añadió `body.rt-future-hub .tema,body.rt-future-cluster .tema,body.rt-future-institutional .tema{font-size:12px!important}` en `future-experience-fix-v4.js`. Verificado con `document.body.className` fijado manualmente a la clase de página real (ver nota de reproducibilidad más abajo): `.tema` pasa de 9px a 12px en hub/cluster.

**6. Barra superior rediseñada: buscador prominente + acciones agrupadas.** El buscador, el toggle de tema, el botón de cuenta y los 2 íconos sociales compartían una franja de acciones dimensionada como grilla de 2 columnas fijas, lo que los apretaba entre sí a resoluciones intermedias. Se rediseñó `.rt-nav-actions` en `future-experience-fix-v4.js` como una fila flexible con tres grupos visualmente separados por divisores (`border-left`): (1) el buscador, ahora con `flex:1 1 auto`, más alto (48px), más ancho (hasta 460px), con fondo e borde de acento teal para que destaque; (2) el grupo de preferencias (tema claro/oscuro + modo lectura); (3) cuenta + redes sociales. Se ajustó también `.topbar-in` (más ancho disponible, `max-width:1780px`) para que las 5 secciones de navegación de la portada (Explorar, Medicina Crítica, Medicina Interna, Metodología, Equipo editorial) tuvieran espacio garantizado (`minmax(700px,1fr)`) sin solaparse con la marca — se detectó y corrigió este solapamiento durante la propia verificación de este cambio, antes de publicar. Verificado con capturas a 1920px y 1440px (breakpoint intermedio, layout de 2 filas): sin solapamientos, buscador claramente el elemento más prominente, sin agrupamiento apretado.

**Nota de reproducibilidad para pruebas con navegador automatizado:** durante esta verificación se reconfirmó que el navegador embebido de Claude ejecuta las pestañas con `document.hidden===true`, lo que pausa el `requestAnimationFrame` del que depende `future-experience.js` para asignar las clases de página (`rt-future-home`, `rt-future-trial`, `rt-future-cluster`, etc.) — sin esas clases, ninguna regla de las capas de mayor especificidad aplica y las lecturas de tamaño de fuente dan falsos positivos de "bug". Toda la verificación de esta ronda se hizo con Playwright real (Chromium headless local) sirviendo el sitio con `python -m http.server`, no con el navegador embebido.

**Verificación de no regresión:** las 10 pruebas de `npm run test:browser` (150 ensayos desktop+móvil, auth, controles de portada, filtro de biblioteca, navegación final, lector unificado v4, controles v5, auditoría completa del sitio, contrato de descarga de PDF) y las 22 pruebas + auditoría SEO de `npm run validate` se ejecutaron contra el estado local con todos los cambios de esta ronda aplicados (antes de publicar) — todas en PASS, 0 advertencias.
